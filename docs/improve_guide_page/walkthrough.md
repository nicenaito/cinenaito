# 修正内容の確認

1. **`src/app/guide/page.tsx`**
   - ビジュアルガイドへの刷新と文字サイズの改善。

2. **`src/app/dashboard/dashboard-client.tsx`**
   - **投稿ボタンの追加**: 
     - デスクトップ版: フィルターの隣に「投稿する」ボタンを配置。
     - モバイル版: 画面右下に固定配置される浮遊アクションボタン（FAB）を追加。
     - デザイン: シネマゴールドのグラデーションとシャドウ（`shadow-cinema-gold/20` 等）を適用し、アプリの世界観に合わせました。
   - 並び替えの簡略化とバグ修正。

3. **`src/components/header.tsx`**
   - 重複を避けるため、ヘッダーから「投稿」ボタンを削除しました。
   - 使用しなくなった `Plus` アイコンのインポートを削除しました。