# タスク

ダッシュボードにおける映画予定リストの並び替え（「自分も観るが多い順」「新しい投稿順」「公開日の早い順」）が正しく機能しない問題を修正する。

- サーバー側で常に「リアクション数順」で最大60件を取得しているため、クライアントサイドでの並び替えでは全件を対象とした正しいソート結果（特に「新しい投稿順」等）にならない問題を解決する。
- 並び替え状態をURLのクエリパラメータとして保持し、リロード時やリンク共有時にも状態が復元されるようにする。